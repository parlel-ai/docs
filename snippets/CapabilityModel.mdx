export const CapabilityModel = () => {
  const caps = [
    { label: "Knowledge",      sub: "Answers from your content",  cls: "tf ts tt tu" },
    { label: "Authentication", sub: "Progressive identity tiers", cls: "cf2 cs crt cu" },
    { label: "Mandates",       sub: "Negotiating authority",      cls: "af as at au" },
    { label: "Campaigns",      sub: "Fixed time-bound offers",    cls: "af as at au" },
    { label: "Actions",        sub: "Verified execution",         cls: "bf bs bt bu" },
    { label: "Fallback",       sub: "Escalation and redirect",    cls: "gf gs gt gu" },
  ]
  const capH = 40, capGap = 8, capW = 168, capTop = 342
  const lx = 66, rx = 446
  return (
    <svg width="100%" viewBox="0 0 680 840" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Parlel capability model" style={{display:"block"}}>
      <title>Parlel capability model</title>
      <style>{`
        .cm text { font-family: ui-sans-serif, system-ui, sans-serif; }
        .cm .lp { font-size: 13px; font-weight: 600; }
        .cm .ls { font-size: 11px; }
        .cm .lx { font-size: 10px; fill: #888780; }
        .cm .conn { stroke-width: 1.5; fill: none; stroke: #888780; }
        .cm .gf { fill: #F1EFE8; } .cm .gs { stroke: #B4B2A9; }
        .cm .gt { fill: #2C2C2A; } .cm .gu { fill: #5F5E5A; }
        .cm .pf { fill: #EEEDFE; } .cm .ps { stroke: #AFA9EC; }
        .cm .pt { fill: #26215C; } .cm .pu { fill: #534AB7; }
        .cm .tf { fill: #E1F5EE; } .cm .ts { stroke: #5DCAA5; }
        .cm .tt { fill: #085041; } .cm .tu { fill: #0F6E56; }
        .cm .af { fill: #FAEEDA; } .cm .as { stroke: #EF9F27; }
        .cm .at { fill: #412402; } .cm .au { fill: #633806; }
        .cm .cf2{ fill: #FAECE7; } .cm .cs { stroke: #F0997B; }
        .cm .crt{ fill: #4A1B0C; } .cm .cu { fill: #712B13; }
        .cm .bf { fill: #E6F1FB; } .cm .bs { stroke: #85B7EB; }
        .cm .bt { fill: #042C53; } .cm .bu { fill: #0C447C; }
      `}</style>
      <defs>
        <marker id="cm-arr" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
          <path d="M2 1L8 5L2 9" fill="none" stroke="#888780" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
        </marker>
      </defs>
      <g className="cm">
        {/* User input */}
        <rect x="240" y="20" width="200" height="44" rx="8" className="gf gs" strokeWidth="0.5"/>
        <text className="lp gt" x="340" y="42" textAnchor="middle" dominantBaseline="central">User input</text>
        <line x1="340" y1="64" x2="340" y2="88" className="conn" markerEnd="url(#cm-arr)"/>
        {/* Intent classification */}
        <rect x="200" y="90" width="280" height="44" rx="8" className="pf ps" strokeWidth="0.5"/>
        <text className="lp pt" x="340" y="107" textAnchor="middle" dominantBaseline="central">Intent classification</text>
        <text className="ls pu" x="340" y="123" textAnchor="middle" dominantBaseline="central">AI evaluates the message</text>
        {/* Split lines */}
        <path d="M200 112 L150 112 L150 168" className="conn" markerEnd="url(#cm-arr)"/>
        <path d="M480 112 L530 112 L530 168" className="conn" markerEnd="url(#cm-arr)"/>
        <text className="lx" x="150" y="105" textAnchor="middle">journey matched</text>
        <text className="lx" x="530" y="105" textAnchor="middle">no journey matched</text>
        {/* Journey activated */}
        <rect x="66" y="170" width="168" height="44" rx="8" className="pf ps" strokeWidth="0.5"/>
        <text className="lp pt" x="150" y="187" textAnchor="middle" dominantBaseline="central">Journey activated</text>
        <text className="ls pu" x="150" y="203" textAnchor="middle" dominantBaseline="central">Intent confirmed</text>
        <line x1="150" y1="214" x2="150" y2="238" className="conn" markerEnd="url(#cm-arr)"/>
        {/* Stage orchestration */}
        <rect x="52" y="240" width="196" height="52" rx="8" className="pf ps" strokeWidth="0.5"/>
        <text className="lp pt" x="150" y="261" textAnchor="middle" dominantBaseline="central">Journey stage</text>
        <text className="lp pt" x="150" y="279" textAnchor="middle" dominantBaseline="central">orchestration</text>
        <line x1="150" y1="292" x2="150" y2="316" className="conn" markerEnd="url(#cm-arr)"/>
        {/* Dashed container */}
        <rect x="52" y="304" width="196" height="426" rx="10" fill="none" stroke="#888780" strokeWidth="1" strokeDasharray="5 4" opacity="0.4"/>
        <text className="lx" x="150" y="330" textAnchor="middle" opacity="0.6">Stage-gated capabilities</text>
        {/* Left caps */}
        {caps.map((c, i) => {
          const y = capTop + i * (capH + capGap)
          const [f,s,t,u] = c.cls.split(" ")
          return (
            <g key={`l${i}`}>
              <rect x={lx} y={y} width={capW} height={capH} rx="6" className={`${f} ${s}`} strokeWidth="0.5"/>
              <text className={`lp ${t}`} x={lx+8} y={y+13} dominantBaseline="central">{c.label}</text>
              <text className={`ls ${u}`} x={lx+8} y={y+29} dominantBaseline="central">{c.sub}</text>
            </g>
          )
        })}
        <text className="lx" x="150" y="656" textAnchor="middle" opacity="0.4">access governed by active stage guardrails</text>
        {/* General */}
        <rect x="446" y="170" width="168" height="44" rx="8" className="pf ps" strokeWidth="0.5"/>
        <text className="lp pt" x="530" y="187" textAnchor="middle" dominantBaseline="central">General</text>
        <text className="ls pu" x="530" y="203" textAnchor="middle" dominantBaseline="central">No journey context</text>
        <line x1="530" y1="214" x2="530" y2="316" className="conn" markerEnd="url(#cm-arr)"/>
        {/* Solid container */}
        <rect x="432" y="304" width="196" height="426" rx="10" fill="none" stroke="#888780" strokeWidth="1" opacity="0.25"/>
        <text className="lx" x="530" y="330" textAnchor="middle" opacity="0.6">Always available</text>
        {/* Right caps */}
        {caps.map((c, i) => {
          const y = capTop + i * (capH + capGap)
          const [f,s,t,u] = c.cls.split(" ")
          return (
            <g key={`r${i}`}>
              <rect x={rx} y={y} width={capW} height={capH} rx="6" className={`${f} ${s}`} strokeWidth="0.5"/>
              <text className={`lp ${t}`} x={rx+8} y={y+13} dominantBaseline="central">{c.label}</text>
              <text className={`ls ${u}`} x={rx+8} y={y+29} dominantBaseline="central">{c.sub}</text>
            </g>
          )
        })}
        {/* Convergence */}
        <path d="M150 730 L150 770 L340 770" className="conn"/>
        <path d="M530 730 L530 770 L340 770" className="conn"/>
        <line x1="340" y1="770" x2="340" y2="792" className="conn" markerEnd="url(#cm-arr)"/>
        {/* Response */}
        <rect x="230" y="794" width="220" height="44" rx="8" className="gf gs" strokeWidth="0.5"/>
        <text className="lp gt" x="340" y="811" textAnchor="middle" dominantBaseline="central">Response to customer</text>
        <text className="ls gu" x="340" y="827" textAnchor="middle" dominantBaseline="central">Delivered in their AI tool</text>
      </g>
    </svg>
  )
}
